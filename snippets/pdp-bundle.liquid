{% assign bundle_product = all_products[bundle] %}
{% if bundle_product != blank %}
  <div class="product-bundle{% if hidden %} hidden{% endif %}">
    <div class="product-bundle__wrapper">
      {% assign current_variant = bundle_product.first_available_variant %}
      {% assign bundle_img = current_variant.featured_image %}
      {% if bundle_img == blank %}
        {% assign bundle_img = bundle_product.featured_image %}
      {% endif %}
      <img src="{{ bundle_img | image_url: width: 300 }}" class="product-bundle__image" />
      <div class="product-bundle__info">
        <p class="product-bundle__title">{{ bundle_product.title | escape }}</p>
        <div class="product-bundle__price">
          {%- render 'price', product: bundle_product, use_variant: true, show_badges: true -%}
          {%- render 'icon-question-circle' %}
        </div>
        {%- render 'pdp-bundle-variant' product: bundle_product -%}
      </div>
    </div>
    <div class="product-bundle__modal hidden">
      {%- render 'pdp-bundle-modal' product: bundle_product %}
    </div>
  </div>
{% endif %}